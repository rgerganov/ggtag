#include "rfid.h"

bool program_em_rfid(uint8_t mfr_id, uint32_t uid)
{
    // this function does nothing on the host
    return true;
}

bool program_hid_rfid(uint16_t id1, uint32_t id2)
{
    // this function does nothing on the host
    return true;
}